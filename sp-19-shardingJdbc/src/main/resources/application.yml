server:
  port: 8018
  servlet:
    context-path: /db
    session:
      timeout: 86400 # session

spring:
  application:
    name: sp-19-shardingJdbc
  profiles:
    active: ds-dev
  main:
    allow-bean-definition-overriding: true
